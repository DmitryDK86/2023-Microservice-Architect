apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-simple-ws.fullname" . }}-test-connection"
  labels:
    {{- include "app-simple-ws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-simple-ws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
