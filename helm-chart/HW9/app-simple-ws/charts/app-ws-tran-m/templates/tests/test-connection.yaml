apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-ws-tran-m.fullname" . }}-test-connection"
  labels:
    {{- include "app-ws-tran-m.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-ws-tran-m.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
