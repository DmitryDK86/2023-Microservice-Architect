apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-ws-tran-m.fullname" . }}
  labels:
    {{- include "app-ws-tran-m.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "app-ws-tran-m.selectorLabels" . | nindent 4 }}
