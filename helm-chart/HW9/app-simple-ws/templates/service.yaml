apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-simple-ws.fullname" . }}
  labels:
    {{- include "app-simple-ws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "app-simple-ws.selectorLabels" . | nindent 4 }}
